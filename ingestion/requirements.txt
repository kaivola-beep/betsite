pandas>=2.0
numpy>=1.24
typer>=0.9
rich>=13.5
pytest>=7.4
# ingestion re-uses veikkaus/ and heppa/ clients; install their requirements too:
# pip install -r veikkaus/requirements.txt
# pip install -r heppa/requirements.txt
